export default function Period() {
    return (
      <div className="space-y-4">
        <h1 className="text-3xl font-semibold tracking-tight">Period view</h1>
        <p className="text-sm text-slate-400">
          Period standings and per-period stats will go here. This is just a
          placeholder so routing + layout work while we wire up the data.
        </p>
      </div>
    );
  }
  